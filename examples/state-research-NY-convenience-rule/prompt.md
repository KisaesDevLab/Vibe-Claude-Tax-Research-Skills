# Example: NY convenience-of-employer rule

User prompt for the `tax-research-state-income` skill:

```
Use tax-research-state-income. Client lives in New Jersey but
works for a New York-based employer. In 2024, worked entirely
from home in NJ; never set foot in the NY office. NY employer
issued W-2 with NY wages. Client believes only NJ should tax the
income because the work was performed in NJ.

Question: Can NY tax the income? How does NY's convenience-of-
employer rule apply?
```

Expected output: see `expected-output.json`.
